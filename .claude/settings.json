{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); file_path=$(echo \"$input\" | jq -r \".tool_input.file_path // empty\"); if [[ \"$file_path\" == *.py ]]; then (cd \"$CLAUDE_PROJECT_DIR\" && uv run ruff check --fix --quiet \"$file_path\" 2>/dev/null && uv run ruff format --quiet \"$file_path\" 2>/dev/null); fi; exit 0'",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
